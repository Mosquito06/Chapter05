<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.or.dgit.bigdata.chapter05">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Ex_2_5" />
        <activity
            android:name=".Chapter06.Ch06MainActivity"
            android:theme="@style/MyAppTheme" />
        <activity
            android:name=".Chapter06.Ch06Hierarchy"
            android:theme="@style/MyNoBar" />
        <activity
            android:name=".AnimationActivity"
            android:theme="@style/MyAnimTheme" />
        <activity android:name=".TextSwitcherActivity" />
        <activity android:name=".ButtonAnimationActivity" />
        <activity android:name=".LanguageActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            ></activity>
    </application>

</manifest>